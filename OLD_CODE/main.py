import manager

quit = False
manager = manager.manager()

while not quit:
    # Check for button presses
    # QUERY BUTTON -> Calls query function -> visualizes what it returns
    # Refresh button -> Call refresh function